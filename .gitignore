# XAMPP / môi trường local
.env
*.log

# Log files dạng JSON (Claude Code, debug)
logs.*.json
debug-*.log

# Composer dependencies (Railway tự chạy composer install)
/vendor/

# Google OAuth credentials (chứa secret, không commit)
client_secret*.json

# Temp fix scripts (chỉ dùng 1 lần, không cần deploy)
fix_*_temp.php
reset_*_temp.php

# Claude Code internal memory (không cần thiết trên git)
.claude/

# Node
node_modules/
package-lock.json

# Hệ thống
.DS_Store
Thumbs.db
desktop.ini
