# Chặn toàn bộ truy cập HTTP vào thư mục này
# Tương thích cả Apache 2.2 và 2.4

# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 (fallback)
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>